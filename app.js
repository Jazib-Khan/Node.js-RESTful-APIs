console.log(); // global

setTimeout(); // calls function after delay

clearTimeout(); // cancels setTimeout()

setInterval(); // calls function repeatedly after delay

clearInterval(); // cancels setInterval()

// ^^ Global objects in JavaScript

window // global object in browser e.g. window.console.log